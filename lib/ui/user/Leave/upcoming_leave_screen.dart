import 'package:flutter/cupertino.dart';

class UpcomingLeaveScreen extends StatelessWidget {
  const UpcomingLeaveScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
