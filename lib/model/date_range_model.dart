class SelectedDateRange {
   DateTime selectedDate;
   DateTime? startDate;
   DateTime? endDate;

   SelectedDateRange({
     required this.selectedDate,
      this.startDate,
      this. endDate,
});
}